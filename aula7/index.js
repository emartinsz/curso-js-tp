//nao podemos criar constantes com palavras reservadas, precisam ter nomes significativos, e nao podem comecar com numeros, e nao podem ter espacos, e nao podem ter caracteres especiais, e nao podem ser iguais a outras constantes ja criadas

// preciso osemore colocar o valor da constante antes de iniciar o codigo, senao da erro


const nome = "Emily"; // Constant é uma variavel que nao pode ser alterada, ou seja, o valor dela nao pode ser modificado, e o valor dela precisa ser definido no momento da criacao da constante, e nao pode ser alterado depois.

// A diferença entre uma constante e uma variavel é que a constante nao pode ser alterada, enquanto a variavel pode ser alterada.

console.log(nome);

const primeiroNumero = 5; // Number
const segundoNumero = 10;
const conta = primeiroNumero * segundoNumero;
console.log(conta);


// ta dentro de aspas é strings e ta fora é Number, e nao podemos fazer operacoes com strings e numbers, pois da erro

console.log( typeof primeiroNumero); // typeof mostra o tipo da variavel

// sinal de soma é usado para concatenar strigs e fazer soma de numeros, e o sinal de menos é usado para subtrair numeros, e nao funciona com strings