// Operadores Aritméticos + adição e concatenção . - subtração * multiplicação / divisão % resto da divisão ** exponenciação

const num1 = 5;
const num2 = 10;
const num3 = 2;
console.log((num1 + num2) / num3); 

let contador = 1;
contador++;
console.log(contador); // esse operador de incremento é o mesmo que contador = contador + 1; 

// temos o pré-incremento e o pós-incremento, o pré-incremento é quando a variável é incrementada antes de ser usada, e o pós-incremento é quando a variável é incrementada depois de ser usada.

